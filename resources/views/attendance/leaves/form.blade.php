@extends('layouts.app')
@section('title',__('app.att.add_leave'))
@section('content')
<section class="page-heading compact"><div><span class="eyebrow">{{ __('app.att.leaves') }}</span><h1>{{ __('app.att.add_leave') }}</h1></div></section><section class="panel form-panel"><form method="POST" action="{{ route('attendance.leaves.store') }}">@csrf<div class="form-grid"><label><span>{{ __('app.att.employee') }}</span><select name="employee_id" required><option value="">{{ __('app.select_placeholder') }}</option>@foreach($employees as $employee)<option value="{{ $employee->id }}">{{ $employee->localizedName() }} · {{ $employee->employee_code }}</option>@endforeach</select></label><label><span>{{ __('app.att.leave_type') }}</span><select name="leave_type">@foreach(['annual','sick','unpaid','business','training'] as $type)<option value="{{ $type }}">{{ __('app.att.leave_'.$type) }}</option>@endforeach</select></label><label><span>{{ __('app.att.date_from') }}</span><input type="date" name="start_date" required></label><label><span>{{ __('app.att.date_to') }}</span><input type="date" name="end_date" required></label><label class="form-span-2"><span>{{ __('app.att.correction_reason') }}</span><textarea name="reason"></textarea></label></div><div class="form-actions"><button class="primary-button">{{ __('app.att.submit_leave') }}</button><a class="ghost-button" href="{{ route('attendance.leaves.index') }}">{{ __('app.cancel') }}</a></div></form></section>
@endsection
